# サプライチェーン対策: 公開から 7 日経っていないバージョンは選ばない。
# 汚染された版の大半は数時間〜1日で発見・削除されるので、少し待つだけで
# 大半の「公開直後にばら撒く」タイプの攻撃をやり過ごせる。
#
# 効くのは npm install / npm update のバージョン解決時だけで、
# lockfile どおりに入れる npm ci には影響しない（CI は今までどおり動く）。
# どうしても出たての版が要るときは `npm i <pkg> --min-release-age 0`。
min-release-age=7
